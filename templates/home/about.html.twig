{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | {{ title }}{% endblock %}

{% block body %}

<div class="container">
    <h1>{{ title }}</h1>
    <nav>
        <a href="{{ path('homepage') }}">Homepage</a>
    </nav>
</div>

{% endblock %}
